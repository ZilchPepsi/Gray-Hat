plugins {
	id 'cpp-application'
}

defaultTasks 'clean', 'install'

task install {
	dependsOn 'build' 
	doLast{
		copy{
			from "$projectDir/src/main/resources/"
			into "$buildDir/install/main/debug/"
		}
		
	}
}
